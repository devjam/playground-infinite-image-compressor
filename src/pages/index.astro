---
import Layout from '../layouts/Layout.astro'
import { title, slug } from '../meta.json'

import InfiniteImageCompressorComponent from '../components/InfiniteImageCompressor'
---

<Layout {title} repoUrl={`https://github.com/devjam/playground-${slug}`}>
  <main>
    <InfiniteImageCompressorComponent client:load />
  </main>
</Layout>
